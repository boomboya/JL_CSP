// JL 7th loops notes

#include <stdio.h>

int main(void){

    char siblings[5][20] = {"jaden", "spencer", "chelsea", "jovie"};
    
    for(int size = 0; size < 4; size++){
        printf("Hello %s\n", siblings[size]);
    }    
        
    return 0;
}